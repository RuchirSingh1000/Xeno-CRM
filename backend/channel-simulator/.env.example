# Channel Simulator environment

# Where to POST webhook callbacks (the CRM API)
CRM_WEBHOOK_URL=http://localhost:8000/webhooks/channel-events

# Shared HMAC secret (must match CRM API)
WEBHOOK_HMAC_SECRET=dev-shared-secret-change-me

APP_ENV=local
APP_PORT=8001
